import type { Lesson } from './lessons';

export const lessonsBatch6: Record<number, Lesson> = {
127: { id:127, title:'单词接龙', summary:'在字典中逐次只改一个字母，求从起点到终点的最短序列长度。', constraints:['所有单词长度相同，且每步新词必须在字典中。','若终点不在字典或无法到达，应返回 0。'], examples:[{input:'begin="hit", end="cog", words=["hot","dot","dog","lot","log","cog"]',output:'5',explanation:'可走 hit→hot→dot→dog→cog。'}], intuition:'每个单词是图节点，一次合法替换是一条无权边；最短变换次数应由 BFS 按层寻找。', bruteForce:'枚举每条变换序列会反复访问同一单词，分支数随深度指数增长。', approach:['先把词表放入集合并检查终点。','队列保存当前单词与序列长度。','每次枚举单词的每个位置。','尝试替换为 26 个字母生成邻词。','未访问邻词立即移出集合并入队。'], code:'from collections import deque\nclass Solution:\n    def ladderLength(self, beginWord: str, endWord: str, wordList: list[str]) -> int:\n        words = set(wordList)\n        if endWord not in words: return 0\n        q = deque([(beginWord, 1)])\n        words.discard(beginWord)\n        while q:\n            word, d = q.popleft()\n            if word == endWord: return d\n            for i in range(len(word)):\n                for ch in "abcdefghijklmnopqrstuvwxyz":\n                    nxt = word[:i] + ch + word[i+1:]\n                    if nxt in words:\n                        words.remove(nxt); q.append((nxt, d + 1))\n        return 0', walkthrough:{input:'hit 到 cog（词表含 hot,dot,dog,cog）',steps:['队列先放入 (hit,1)。','第一层找到 hot，距离 2。','下一层由 hot 找到 dot，距离 3。','随后找到 dog，再到 cog，距离 5。'],result:'返回 5。'}, complexity:{time:'O(N·L·26·L)，生成并哈希 N 个长度 L 的候选词。',space:'O(N·L)，用于集合和队列。'}, pitfalls:['访问标记要在入队时完成，避免重复入队。','答案是序列中的单词数，起点长度为 1。'], related:['433 最小基因变化','752 打开转盘锁'] },
119: { id:119, title:'杨辉三角 II', summary:'只返回杨辉三角指定的第 rowIndex 行，并把额外空间压缩到一维。', constraints:['行号从 0 开始，第 0 行为 [1]。','每个内部元素等于上一行相邻两数之和。'], examples:[{input:'rowIndex = 3',output:'[1,3,3,1]',explanation:'依次构造到第 3 行即可。'}], intuition:'一维数组若从右向左更新，计算 dp[j] 时 dp[j-1] 仍属于上一行，因此不会覆盖依赖。', bruteForce:'构造前 rowIndex 行的完整二维三角形，能得到答案但浪费 O(k²) 空间。', approach:['创建长度 k+1 的零数组。','令 dp[0]=1。','逐行从 1 迭代到 k。','每行让 j 从行号倒序到 1。','执行 dp[j]+=dp[j-1] 并最终返回。'], code:'class Solution:\n    def getRow(self, rowIndex: int) -> list[int]:\n        dp = [0] * (rowIndex + 1)\n        dp[0] = 1\n        for i in range(1, rowIndex + 1):\n            for j in range(i, 0, -1):\n                dp[j] += dp[j - 1]\n        return dp', walkthrough:{input:'rowIndex = 3',steps:['初始为 [1,0,0,0]。','第 1 行更新为 [1,1,0,0]。','第 2 行倒序更新为 [1,2,1,0]。','第 3 行倒序更新为 [1,3,3,1]。'],result:'返回 [1,3,3,1]。'}, complexity:{time:'O(k²)。',space:'O(k)，只保存目标行数组。'}, pitfalls:['正序更新会混用本行新值。','不要把 rowIndex 当成行长度，长度应为 k+1。'], related:['118 杨辉三角','一维动态规划'] },
377: { id:377, title:'组合总和 IV', summary:'统计由给定正整数构成目标和的有序序列数量，顺序不同视为不同方案。', constraints:['候选数均为正数且互不相同。','结果按题目保证可放入整数范围，目标可为 0。'], examples:[{input:'nums=[1,2,3], target=4',output:'7',explanation:'例如 1+3 与 3+1 是两种方案。'}], intuition:'把最后一个数 x 拿掉后，前缀必须组成 sum-x；对每个总和依次尝试所有末尾即可累计有序方案。', bruteForce:'递归尝试每个位置放任意候选数，会重复计算相同剩余目标，接近指数复杂度。', approach:['定义 dp[s] 为总和 s 的序列数。','设 dp[0]=1 表示空前缀。','按 s 从 1 到 target 枚举。','对每个 x，若 x≤s 则累加 dp[s-x]。','返回 dp[target]。'], code:'class Solution:\n    def combinationSum4(self, nums: list[int], target: int) -> int:\n        dp = [0] * (target + 1)\n        dp[0] = 1\n        for s in range(1, target + 1):\n            for x in nums:\n                if x <= s: dp[s] += dp[s - x]\n        return dp[target]', walkthrough:{input:'nums=[1,2], target=3',steps:['dp[0]=1。','s=1 只有末尾 1，dp[1]=1。','s=2 可接 1 或 2，dp[2]=2。','s=3 从 dp[2] 与 dp[1] 转移，得到 3。'],result:'三种序列为 111、12、21。'}, complexity:{time:'O(target·n)。',space:'O(target)。'}, pitfalls:['外层必须枚举总和，才能统计排列。','dp[0] 必须设为 1，作为转移单位元。'], related:['322 零钱兑换','518 零钱兑换 II'] },
494: { id:494, title:'目标和', summary:'给每个数添加正负号，使表达式结果等于 target，并统计不同添加方式。', constraints:['每个元素都必须选择一次正号或负号。','数组含非负整数，目标可能为负数。'], examples:[{input:'nums=[1,1,1,1,1], target=3',output:'5',explanation:'恰有五种符号分配得到 3。'}], intuition:'设正号元素和为 P、总和为 S，则 P-(S-P)=target，所以转化为选子集凑 (S+target)/2。', bruteForce:'对 n 个位置分别尝试正负号，共检查 2^n 种表达式。', approach:['求数组总和 S。','若 |target|>S 或 S+target 为奇数则返回 0。','令 goal=(S+target)/2。','用一维背包从 goal 向下更新计数。','dp[0]=1，处理完所有数后返回 dp[goal]。'], code:'class Solution:\n    def findTargetSumWays(self, nums: list[int], target: int) -> int:\n        total = sum(nums)\n        if abs(target) > total or (total + target) % 2: return 0\n        goal = (total + target) // 2\n        dp = [0] * (goal + 1); dp[0] = 1\n        for x in nums:\n            for s in range(goal, x - 1, -1):\n                dp[s] += dp[s - x]\n        return dp[goal]', walkthrough:{input:'nums=[1,1,2], target=2',steps:['总和 S=4。','正数子集目标为 (4+2)/2=3。','处理两个 1 后，和为 1 的选择有两种。','加入 2 后，和为 3 的选择有两种。'],result:'返回 2。'}, complexity:{time:'O(n·goal)。',space:'O(goal)。'}, pitfalls:['需先排除 target 绝对值超过总和。','0 也要倒序处理，它会让方案数翻倍。'], related:['416 分割等和子集','1049 最后一块石头的重量 II'] },
518: { id:518, title:'零钱兑换 II', summary:'给定不同面额，统计凑成总金额的无序组合数，每种硬币可重复使用。', constraints:['硬币面额均为正数且互不相同。','组合不区分取币顺序，金额为 0 时有一种空组合。'], examples:[{input:'amount=5, coins=[1,2,5]',output:'4',explanation:'组合为 5、2+2+1、2+1+1+1、五个 1。'}], intuition:'按硬币种类逐个开放，再让金额正序更新，能重复使用当前硬币且不会把同一组硬币按顺序重复计数。', bruteForce:'递归枚举每种硬币取 0 到若干枚，存在大量相同“种类下标+剩余金额”子问题。', approach:['创建 dp[0..amount]。','令 dp[0]=1。','外层逐个遍历硬币 coin。','内层金额从 coin 正序到 amount。','累加 dp[s-coin] 到 dp[s] 并返回终点。'], code:'class Solution:\n    def change(self, amount: int, coins: list[int]) -> int:\n        dp = [0] * (amount + 1); dp[0] = 1\n        for coin in coins:\n            for s in range(coin, amount + 1):\n                dp[s] += dp[s - coin]\n        return dp[amount]', walkthrough:{input:'amount=4, coins=[1,2]',steps:['初始 dp[0]=1。','开放硬币 1 后每个金额均有一种。','开放硬币 2 时 dp[2] 增加一种。','dp[4] 从旧方案与 dp[2] 累加为 3。'],result:'组合是 1111、112、22。'}, complexity:{time:'O(amount·n)。',space:'O(amount)。'}, pitfalls:['交换两层循环会统计排列而非组合。','完全背包金额必须正序更新。'], related:['322 零钱兑换','377 组合总和 Ⅳ'] },
1049: { id:1049, title:'最后一块石头的重量 II', summary:'把石头分成两组，使两组总重量之差最小。', constraints:['每块石头只能归入一组且使用一次。','碰撞过程可等价为给重量添加正负号。'], examples:[{input:'stones=[2,7,4,1,8,1]',output:'1',explanation:'可分为重量 11 与 12 的两组。'}], intuition:'总和为 S 时，只要找不超过 S/2 的最大可达子集和 best，最终差就是 S-2·best。', bruteForce:'枚举每块石头属于哪一组，共有 2^n 种划分。', approach:['计算总重量 total 与容量 total//2。','用布尔 dp 表示容量内可达和。','初始化 dp[0]=True。','每块石头让和从容量向下更新。','从容量向下找最大可达值并计算差。'], code:'class Solution:\n    def lastStoneWeightII(self, stones: list[int]) -> int:\n        total = sum(stones); cap = total // 2\n        dp = [False] * (cap + 1); dp[0] = True\n        for x in stones:\n            for s in range(cap, x - 1, -1):\n                dp[s] = dp[s] or dp[s - x]\n        best = max(s for s in range(cap + 1) if dp[s])\n        return total - 2 * best', walkthrough:{input:'stones=[2,3,4]',steps:['总和为 9，容量为 4。','加入 2 后可达 2。','加入 3 后可达 3。','加入 4 后最佳半和为 4。'],result:'两组差为 9-8=1。'}, complexity:{time:'O(n·S)。',space:'O(S)，S 此处表示总重量。'}, pitfalls:['容量只需到总和的一半。','01 背包必须倒序，避免同一石头重复使用。'], related:['416 分割等和子集','494 目标和'] },
647: { id:647, title:'回文子串', summary:'统计字符串中所有回文连续子串，位置不同即视为不同。', constraints:['统计的是连续子串而非子序列。','奇数和偶数长度的回文都要计入。'], examples:[{input:'s="aaa"',output:'6',explanation:'三个单字符、两个 aa 和一个 aaa。'}], intuition:'每个回文都拥有唯一中心；中心可能是字符，也可能是两个字符之间的缝隙，从中心向两侧扩展即可不重不漏。', bruteForce:'枚举 O(n²) 个子串并逐个反转或双指针检查，总计 O(n³)。', approach:['把每个字符视为奇回文中心。','把每个相邻缝隙视为偶回文中心。','设置左右指针从中心出发。','字符相等时计数并向外扩。','越界或字符不同后切换下一个中心。'], code:'class Solution:\n    def countSubstrings(self, s: str) -> int:\n        def expand(l, r):\n            count = 0\n            while l >= 0 and r < len(s) and s[l] == s[r]:\n                count += 1; l -= 1; r += 1\n            return count\n        return sum(expand(i, i) + expand(i, i + 1) for i in range(len(s)))', walkthrough:{input:'s="aba"',steps:['以 0 为中心得到 a。','以 1 为中心先得到 b。','继续扩展得到 aba。','以 2 为中心得到 a，缝隙中心均失败。'],result:'共 4 个回文子串。'}, complexity:{time:'O(n²)。',space:'O(1)，不计生成器开销。'}, pitfalls:['不要漏掉偶数长度中心。','相同文本出现在不同位置要分别计数。'], related:['5 最长回文子串','516 最长回文子序列'] },
516: { id:516, title:'最长回文子序列', summary:'求字符串中保持相对顺序但可跳过字符的最长回文序列长度。', constraints:['子序列不要求连续。','空区间长度为 0，单字符区间长度为 1。'], examples:[{input:'s="bbbab"',output:'4',explanation:'可选择子序列 bbbb。'}], intuition:'区间两端相等时可共同加入答案；不等时至少舍弃一端，取两个更小区间的较优值。', bruteForce:'枚举全部 2^n 个子序列，再逐个判断是否回文。', approach:['定义 dp[i][j] 为闭区间最长回文子序列。','把所有 dp[i][i] 初始化为 1。','让 i 从右向左遍历。','让 j 从 i+1 向右遍历。','端点相等加 2，否则取舍一端的最大值。'], code:'class Solution:\n    def longestPalindromeSubseq(self, s: str) -> int:\n        n = len(s); dp = [[0] * n for _ in range(n)]\n        for i in range(n - 1, -1, -1):\n            dp[i][i] = 1\n            for j in range(i + 1, n):\n                if s[i] == s[j]: dp[i][j] = dp[i + 1][j - 1] + 2\n                else: dp[i][j] = max(dp[i + 1][j], dp[i][j - 1])\n        return dp[0][n - 1]', walkthrough:{input:'s="cbbd"',steps:['单字符区间先置为 1。','区间 bb 两端相等，长度为 2。','区间 bbd 两端不同，保留内部最优 2。','完整 cbbd 两端不同，答案仍为 2。'],result:'返回 2。'}, complexity:{time:'O(n²)。',space:'O(n²)。'}, pitfalls:['不要误写成连续回文子串。','遍历顺序必须保证较小区间已计算。'], related:['1143 最长公共子序列','647 回文子串'] },
10: { id:10, title:'正则表达式匹配', summary:'判断整个字符串是否匹配只含普通字符、点号与星号的模式。', constraints:['点号匹配任意单字符，星号表示其前一元素重复零次或多次。','要求覆盖整个字符串，而不是寻找局部匹配。'], examples:[{input:'s="aab", p="c*a*b"',output:'true',explanation:'c* 取零次，a* 取两次，再匹配 b。'}], intuition:'遇到星号时有两种选择：让“前元素+星号”整体出现零次，或当前字符匹配后继续停留在星号模式上。', bruteForce:'对每个星号枚举重复次数并回溯，多个星号会造成指数分支。', approach:['定义 dfs(i,j) 表示两个后缀能否完全匹配。','若模式耗尽，仅字符串也耗尽才成功。','计算当前字符是否与模式字符匹配。','若下一位是星号，尝试跳过该组或消费一个字符。','否则双方各前进一步，并缓存所有状态。'], code:'from functools import cache\nclass Solution:\n    def isMatch(self, s: str, p: str) -> bool:\n        @cache\n        def dfs(i, j):\n            if j == len(p): return i == len(s)\n            first = i < len(s) and (p[j] == s[i] or p[j] == ".")\n            if j + 1 < len(p) and p[j + 1] == "*":\n                return dfs(i, j + 2) or (first and dfs(i + 1, j))\n            return first and dfs(i + 1, j + 1)\n        return dfs(0, 0)', walkthrough:{input:'s="aa", p="a*"',steps:['状态 (0,0) 看到星号。','跳过 a* 会剩余字符串，失败。','消费首个 a 后仍停在模式位置 0。','再消费一个 a，随后跳过 a*，双方耗尽。'],result:'返回 true。'}, complexity:{time:'O(mn)，每个后缀状态只计算一次。',space:'O(mn)，缓存与递归栈。'}, pitfalls:['星号修饰前一个元素，不能独立匹配。','消费字符时模式索引不能越过星号组。'], related:['44 通配符匹配','动态规划字符串匹配'] },
44: { id:44, title:'通配符匹配', summary:'用含问号和星号的通配模式匹配完整字符串，其中星号可覆盖任意字符序列。', constraints:['问号恰好匹配一个字符。','星号可匹配空串或任意长度字符，要求整串匹配。'], examples:[{input:'s="adceb", p="*a*b"',output:'true',explanation:'两个星号分别吸收空前缀和 dce。'}], intuition:'记录最近一个星号；后续失配时，让该星号比上次多吞一个字符，再从星号后的模式重试。', bruteForce:'为每个星号尝试所有可能长度，嵌套分支最坏指数级。', approach:['用 i、j 扫描字符串与模式。','字符相同或遇问号时双方前进。','遇星号时记住其位置与当前字符串起点。','失配且曾见星号时扩展星号覆盖范围。','无法回退则失败，最后跳过模式尾部星号。'], code:'class Solution:\n    def isMatch(self, s: str, p: str) -> bool:\n        i = j = 0; star = -1; matched = 0\n        while i < len(s):\n            if j < len(p) and (p[j] == "?" or p[j] == s[i]): i += 1; j += 1\n            elif j < len(p) and p[j] == "*": star = j; matched = i; j += 1\n            elif star != -1: matched += 1; i = matched; j = star + 1\n            else: return False\n        while j < len(p) and p[j] == "*": j += 1\n        return j == len(p)', walkthrough:{input:'s="abc", p="a*c"',steps:['a 与 a 直接匹配。','记录星号位置，先让它匹配空串。','b 与 c 失配，回到星号并多吞 b。','随后 c 与 c 匹配且双方耗尽。'],result:'返回 true。'}, complexity:{time:'O(m+n) 的常见贪心扫描，回退次数受字符串长度限制。',space:'O(1)。'}, pitfalls:['本题星号与正则题含义不同，它不依赖前一字符。','字符串耗尽后模式剩余部分必须全是星号。'], related:['10 正则表达式匹配','双指针贪心'] },
312: { id:312, title:'戳气球', summary:'选择戳破气球的顺序，使每次由当前相邻值产生的金币总和最大。', constraints:['戳破后左右剩余气球会成为新邻居。','边界外视作数值 1。'], examples:[{input:'nums=[3,1,5,8]',output:'167',explanation:'合适顺序可得到最大金币 167。'}], intuition:'正向选择第一个气球时邻居会变化；反向固定某区间最后被戳的气球，它的邻居恰是区间两端，子问题便独立。', bruteForce:'枚举 n! 种戳破顺序并模拟邻接变化。', approach:['在数组两端补 1。','定义 dp[l][r] 为开区间内的最大金币。','按区间长度从小到大计算。','枚举 k 作为该区间最后戳破的气球。','合并左右子区间与边界乘积并取最大值。'], code:'class Solution:\n    def maxCoins(self, nums: list[int]) -> int:\n        a = [1] + nums + [1]; n = len(a)\n        dp = [[0] * n for _ in range(n)]\n        for gap in range(2, n):\n            for l in range(n - gap):\n                r = l + gap\n                for k in range(l + 1, r):\n                    dp[l][r] = max(dp[l][r], dp[l][k] + a[l] * a[k] * a[r] + dp[k][r])\n        return dp[0][n - 1]', walkthrough:{input:'nums=[3,1]',steps:['补边界后为 [1,3,1,1]。','单气球区间先计算：3 得 3，1 得 3。','完整区间尝试最后戳 3，合计 6。','尝试最后戳 1，合计 4，保留 6。'],result:'最大金币为 6。'}, complexity:{time:'O(n³)。',space:'O(n²)。'}, pitfalls:['状态最好使用开区间，边界乘积更清晰。','转移枚举的是最后戳破者，不是第一个。'], related:['区间动态规划','1039 多边形三角剖分的最低得分'] },
96: { id:96, title:'不同的二叉搜索树', summary:'计算 1 到 n 能组成多少种结构不同的二叉搜索树。', constraints:['节点值恰好使用 1 到 n 且各一次。','只统计结构与相对值次序，不需要生成树。'], examples:[{input:'n=3',output:'5',explanation:'分别选择三个值为根并组合左右子树，共五种。'}], intuition:'选第 r 个值作根时，左侧有 r-1 个较小值，右侧有 n-r 个较大值，两边结构数相乘。', bruteForce:'实际生成全部树再计数，需要构造大量节点且无法只保留数值答案。', approach:['令 dp[k] 表示 k 个有序值的 BST 数。','初始化 dp[0]=dp[1]=1。','从节点数 2 递增计算。','枚举左子树节点数 left。','累加 dp[left]·dp[k-1-left]。'], code:'class Solution:\n    def numTrees(self, n: int) -> int:\n        dp = [0] * (n + 1); dp[0] = 1\n        if n >= 1: dp[1] = 1\n        for nodes in range(2, n + 1):\n            for left in range(nodes):\n                dp[nodes] += dp[left] * dp[nodes - 1 - left]\n        return dp[n]', walkthrough:{input:'n=3',steps:['dp[0]=1、dp[1]=1。','两个节点可让左侧规模为 0 或 1，dp[2]=2。','三个节点枚举左侧规模 0、1、2。','贡献为 2、1、2，总计 5。'],result:'返回 5。'}, complexity:{time:'O(n²)。',space:'O(n)。'}, pitfalls:['空子树有一种结构，dp[0] 不能为 0。','左右子树组合数应相乘，再对根选择求和。'], related:['95 不同的二叉搜索树 II','卡特兰数'] },
547: { id:547, title:'省份数量', summary:'在城市相连矩阵中统计连通分量数量，每个分量代表一个省份。', constraints:['连接关系对称且城市与自身相连。','间接可达的城市也属于同一省份。'], examples:[{input:'isConnected=[[1,1,0],[1,1,0],[0,0,1]]',output:'2',explanation:'前两个城市一组，第三个城市单独一组。'}], intuition:'每遇到一个未访问城市，就发现了一个新连通分量；从它 DFS 可一次标记整个省份。', bruteForce:'反复检查所有城市对并手工传递可达关系，会做大量无效扫描。', approach:['建立 visited 数组。','依次检查每个城市。','若未访问则省份数加一并启动 DFS。','DFS 扫描该城市矩阵行中的所有邻城。','递归标记未访问邻城，最后返回计数。'], code:'class Solution:\n    def findCircleNum(self, isConnected: list[list[int]]) -> int:\n        n = len(isConnected); seen = [False] * n\n        def dfs(u):\n            seen[u] = True\n            for v in range(n):\n                if isConnected[u][v] and not seen[v]: dfs(v)\n        ans = 0\n        for city in range(n):\n            if not seen[city]: ans += 1; dfs(city)\n        return ans', walkthrough:{input:'[[1,1,0],[1,1,0],[0,0,1]]',steps:['城市 0 未访问，省份数变 1。','DFS 从 0 标记城市 1。','扫描到城市 1 时已访问，跳过。','城市 2 未访问，省份数变 2。'],result:'返回 2。'}, complexity:{time:'O(n²)，矩阵每行至多扫描一次。',space:'O(n)，访问数组与递归栈。'}, pitfalls:['矩阵给的是直接连接，还需考虑传递可达。','计数应发生在启动一次新搜索时。'], related:['200 岛屿数量','1971 寻找图中是否存在路径'] },
684: { id:684, title:'冗余连接', summary:'在一棵树新增一条边后，找出使图出现环的那条输入边。', constraints:['节点编号从 1 开始，边按给定顺序处理。','若存在多个候选，返回输入中最后满足要求的边；题设结构保证直接扫描可确定。'], examples:[{input:'edges=[[1,2],[1,3],[2,3]]',output:'[2,3]',explanation:'前两条已连通三个点，第三条闭合成环。'}], intuition:'逐边建森林；若一条边的两个端点此前已经连通，加入它就恰好形成环。', bruteForce:'对每条候选边删除后再 DFS 判断剩余图是否为树，需重复建图和遍历。', approach:['初始化每个节点的并查集父亲。','find 使用路径压缩寻找代表元。','按输入顺序处理每条边。','若两端代表元相同，立即返回该边。','否则合并两个集合并继续。'], code:'class Solution:\n    def findRedundantConnection(self, edges: list[list[int]]) -> list[int]:\n        parent = list(range(len(edges) + 1))\n        def find(x):\n            while x != parent[x]:\n                parent[x] = parent[parent[x]]; x = parent[x]\n            return x\n        for u, v in edges:\n            ru, rv = find(u), find(v)\n            if ru == rv: return [u, v]\n            parent[ru] = rv\n        return []', walkthrough:{input:'[[1,2],[2,3],[1,3]]',steps:['初始每个节点自成集合。','合并 1 与 2。','再合并 2 与 3，此时三点同组。','处理 1-3 时代表元相同，检测到环。'],result:'返回 [1,3]。'}, complexity:{time:'O(n·α(n))，近似线性。',space:'O(n)。'}, pitfalls:['节点编号从 1 开始，父数组要多一个位置。','应按输入顺序处理，首次闭环边就是答案。'], related:['721 账户合并','990 等式方程的可满足性'] },
721: { id:721, title:'账户合并', summary:'把共享任一邮箱的账户合并，并输出姓名与排序后的邮箱列表。', constraints:['同一人的账户名称一致，身份由邮箱连通关系决定。','邮箱可能通过多条账户记录间接相连。'], examples:[{input:'[["John","a@mail","b@mail"],["John","b@mail","c@mail"]]',output:'[["John","a@mail","b@mail","c@mail"]]',explanation:'两条记录共享 b@mail。'}], intuition:'把邮箱视为节点，同一账户内邮箱相连；最终每个连通分量就是一个合并账户。', bruteForce:'不断两两比较账户邮箱集合并合并重叠项，反复扫描可能达到高次复杂度。', approach:['为每个首次出现的邮箱建立并查集节点并记录姓名。','把同一账户的所有邮箱与第一个邮箱合并。','遍历所有邮箱并按根节点分组。','对每组邮箱做字典序排序。','在组首添加对应姓名并收集结果。'], code:'from collections import defaultdict\nclass Solution:\n    def accountsMerge(self, accounts: list[list[str]]) -> list[list[str]]:\n        parent = {} ; owner = {}\n        def find(x):\n            parent.setdefault(x, x)\n            if parent[x] != x: parent[x] = find(parent[x])\n            return parent[x]\n        for account in accounts:\n            name, first = account[0], account[1]\n            for email in account[1:]:\n                owner[email] = name; parent.setdefault(email, email)\n                parent[find(email)] = find(first)\n        groups = defaultdict(list)\n        for email in parent: groups[find(email)].append(email)\n        return [[owner[root]] + sorted(emails) for root, emails in groups.items()]', walkthrough:{input:'John: a,b 与 John: b,c',steps:['为 a、b 建节点并将二者合并。','第二条记录让 b 与 c 合并。','路径压缩后 a、b、c 根相同。','分组并排序邮箱，再在前面放 John。'],result:'得到一个含 a、b、c 的账户。'}, complexity:{time:'O(E·α(E)+E log E)，排序占主要部分。',space:'O(E)，E 为不同邮箱数。'}, pitfalls:['不能只合并直接相邻记录，需处理传递关系。','排序仅针对邮箱，姓名必须放在首位。'], related:['684 冗余连接','并查集连通分量'] },
990: { id:990, title:'等式方程的可满足性', summary:'判断由单字母变量组成的相等与不等关系能否同时成立。', constraints:['每条方程形如 a==b 或 a!=b。','变量仅为小写英文字母。'], examples:[{input:'["a==b","b!=a"]',output:'false',explanation:'相等关系与不等关系直接冲突。'}], intuition:'相等关系具有传递性，先用并查集合并所有等式；随后任何不等式两端同组就构成矛盾。', bruteForce:'枚举 26 个变量的具体取值域没有必要，也无法预先界定需要多少值。', approach:['初始化 26 个变量各自为集合。','第一遍只处理 == 方程。','合并等式两端的代表元。','第二遍处理所有 != 方程。','若不等式两端代表元相同则返回 false，否则返回 true。'], code:'class Solution:\n    def equationsPossible(self, equations: list[str]) -> bool:\n        parent = list(range(26))\n        def find(x):\n            if parent[x] != x: parent[x] = find(parent[x])\n            return parent[x]\n        for e in equations:\n            if e[1] == "=": parent[find(ord(e[0])-97)] = find(ord(e[3])-97)\n        for e in equations:\n            if e[1] == "!" and find(ord(e[0])-97) == find(ord(e[3])-97): return False\n        return True', walkthrough:{input:'["a==b","b==c","a!=c"]',steps:['初始 a、b、c 分属不同集合。','处理 a==b 后二者同组。','处理 b==c 后三者同组。','检查 a!=c 时发现代表元相同。'],result:'返回 false。'}, complexity:{time:'O(m·α(26))，可视为 O(m)。',space:'O(1)，父数组固定为 26。'}, pitfalls:['必须先处理完所有等式再检查不等式。','a!=a 会立即与自身同组产生冲突。'], related:['684 冗余连接','721 账户合并'] },
785: { id:785, title:'判断二分图', summary:'判断无向图能否将节点分成两组，使每条边的两端属于不同组。', constraints:['图可能不连通，邻接表表示无向边。','节点可无邻居，自环会使图不可能二分。'], examples:[{input:'graph=[[1,3],[0,2],[1,3],[0,2]]',output:'true',explanation:'可分为 {0,2} 与 {1,3}。'}], intuition:'给任一点染一种颜色后，它的邻居只能染另一种；传播中若相邻点同色，则不存在合法划分。', bruteForce:'枚举每个节点属于两组中的哪一组，共有 2^n 种染色。', approach:['用 0 表示未染色，±1 表示两种颜色。','遍历所有节点以覆盖非连通图。','未染色节点先染为 1 并入队。','BFS 给每个邻居染相反颜色。','若遇同色相邻节点返回 false，全部完成则 true。'], code:'from collections import deque\nclass Solution:\n    def isBipartite(self, graph: list[list[int]]) -> bool:\n        color = [0] * len(graph)\n        for start in range(len(graph)):\n            if color[start]: continue\n            color[start] = 1; q = deque([start])\n            while q:\n                u = q.popleft()\n                for v in graph[u]:\n                    if color[v] == 0: color[v] = -color[u]; q.append(v)\n                    elif color[v] == color[u]: return False\n        return True', walkthrough:{input:'三角形 0-1-2-0',steps:['先把 0 染为 1。','邻居 1 与 2 都染为 -1。','处理节点 1 时查看邻居 2。','1 与 2 同色且相邻，发生冲突。'],result:'返回 false。'}, complexity:{time:'O(V+E)。',space:'O(V)。'}, pitfalls:['必须从每个未染色节点启动，不能只检查节点 0 所在分量。','检查的是邻边两端异色，不是全局交替编号。'], related:['886 可能的二分法','图染色'] },
743: { id:743, title:'网络延迟时间', summary:'求信号从源点沿有向带权边到达所有节点所需的最短时间。', constraints:['边权为非负时间，节点编号为 1 到 n。','若存在源点不可达节点则返回 -1。'], examples:[{input:'times=[[2,1,1],[2,3,1],[3,4,1]], n=4, k=2',output:'2',explanation:'最晚到达节点 4，最短时间为 2。'}], intuition:'所有边权非负，可用 Dijkstra 每次确定当前距离最小的未定节点；全部最短距离中的最大值就是广播完成时间。', bruteForce:'枚举从源点到各节点的所有路径，环和分支会导致无限或指数数量候选。', approach:['建立有向邻接表。','距离数组置为无穷，源点为 0。','最小堆存放距离与节点。','弹出过期项时跳过，并松弛所有出边。','若仍有无穷距离返回 -1，否则返回最大距离。'], code:'import heapq\nclass Solution:\n    def networkDelayTime(self, times: list[list[int]], n: int, k: int) -> int:\n        graph = [[] for _ in range(n + 1)]\n        for u, v, w in times: graph[u].append((v, w))\n        dist = [float("inf")] * (n + 1); dist[k] = 0\n        heap = [(0, k)]\n        while heap:\n            d, u = heapq.heappop(heap)\n            if d != dist[u]: continue\n            for v, w in graph[u]:\n                nd = d + w\n                if nd < dist[v]: dist[v] = nd; heapq.heappush(heap, (nd, v))\n        ans = max(dist[1:])\n        return -1 if ans == float("inf") else ans', walkthrough:{input:'1→2(1), 1→3(4), 2→3(1)，源 1',steps:['源点距离置 0 并入堆。','弹出 1，更新 dist[2]=1、dist[3]=4。','弹出 2，用路径 1→2→3 把 dist[3] 改为 2。','弹出 3 后结束，最大最短距离为 2。'],result:'返回 2。'}, complexity:{time:'O((V+E) log V)。',space:'O(V+E)。'}, pitfalls:['图是有向图，不能反向加边。','堆中可能有旧距离，必须跳过过期项。'], related:['787 K 站中转内最便宜的航班','Dijkstra 最短路'] },
787: { id:787, title:'K 站中转内最便宜的航班', summary:'在最多经过 k 个中转站的限制下，求起点到终点的最低价格。', constraints:['最多 k 个中转意味着最多使用 k+1 条边。','航班有方向且价格为非负数，不可达时返回 -1。'], examples:[{input:'n=4, flights=[[0,1,100],[1,2,100],[2,3,100],[0,3,500]], src=0,dst=3,k=1',output:'500',explanation:'三段便宜路线超过一站中转限制。'}], intuition:'限制边数后，可进行 k+1 轮 Bellman-Ford；每轮必须从上一轮快照转移，防止同轮使用多条新边。', bruteForce:'DFS 枚举不超过 k+1 条边的全部路径，分支数约为出度的 k 次方。', approach:['初始化 dist，只有源点为 0。','执行 k+1 轮松弛。','每轮复制 dist 为 nextDist。','用旧 dist 遍历所有航班并更新副本。','轮末替换 dist，最终按可达性返回终点值。'], code:'class Solution:\n    def findCheapestPrice(self, n: int, flights: list[list[int]], src: int, dst: int, k: int) -> int:\n        inf = float("inf"); dist = [inf] * n; dist[src] = 0\n        for _ in range(k + 1):\n            nxt = dist.copy()\n            for u, v, price in flights:\n                if dist[u] != inf:\n                    nxt[v] = min(nxt[v], dist[u] + price)\n            dist = nxt\n        return -1 if dist[dst] == inf else dist[dst]', walkthrough:{input:'0→1(100),1→2(100),0→2(500), k=0',steps:['k=0 只允许一条边，所以仅做一轮。','初始只有 dist[0]=0。','由旧数组更新 1 为 100。','同时直达边更新 2 为 500，不能借本轮的新 dist[1]。'],result:'返回 500。'}, complexity:{time:'O((k+1)·E)。',space:'O(V)。'}, pitfalls:['中转站数 k 对应边数 k+1。','若原地更新，会在一轮内错误使用多条边。'], related:['743 网络延迟时间','Bellman-Ford'] },
1584: { id:1584, title:'连接所有点的最小费用', summary:'以曼哈顿距离为边权连接所有二维点，求覆盖全部点的最小生成树费用。', constraints:['任意两点都可以直接连接。','最终网络必须连通，且总费用最小。'], examples:[{input:'points=[[0,0],[2,2],[3,10],[5,2],[7,0]]',output:'20',explanation:'选择合适的四条边可用总费用 20 连通五点。'}], intuition:'完整图边很多，但 Prim 无需显式存边；每次选离当前生成树最近的点，再用它更新其他点的最小接入代价。', bruteForce:'枚举所有边子集并检查是否构成生成树，候选数量指数级。', approach:['维护每个点到当前树的最小距离 minDist。','从点 0 开始，其距离设为 0。','每轮在线性扫描中选未加入且距离最小的点。','把其接入代价加入答案。','用该点到其他未加入点的曼哈顿距离更新 minDist。'], code:'class Solution:\n    def minCostConnectPoints(self, points: list[list[int]]) -> int:\n        n = len(points); used = [False] * n\n        dist = [float("inf")] * n; dist[0] = 0; ans = 0\n        for _ in range(n):\n            u = min((i for i in range(n) if not used[i]), key=lambda i: dist[i])\n            used[u] = True; ans += dist[u]\n            x, y = points[u]\n            for v in range(n):\n                if not used[v]:\n                    d = abs(x - points[v][0]) + abs(y - points[v][1])\n                    dist[v] = min(dist[v], d)\n        return ans', walkthrough:{input:'points=[[0,0],[1,0],[3,0]]',steps:['点 0 以代价 0 入树。','更新点 1 距离 1、点 2 距离 3。','选点 1，累计 1，并把点 2 距离更新为 2。','选点 2，累计费用变为 3。'],result:'返回 3。'}, complexity:{time:'O(n²)。',space:'O(n)。'}, pitfalls:['曼哈顿距离是横纵坐标差绝对值之和。','累计的是点加入时的最小接入代价。'], related:['Prim 最小生成树','684 冗余连接'] },
1631: { id:1631, title:'最小体力消耗路径', summary:'在高度网格中从左上走到右下，最小化路径上最大相邻高度差。', constraints:['每步只能上下左右移动。','路径代价不是差值之和，而是沿途最大差值。'], examples:[{input:'heights=[[1,2,2],[3,8,2],[5,3,5]]',output:'2',explanation:'存在一条每步高度差都不超过 2 的路径。'}], intuition:'把格子看成图节点，边权为高度差；Dijkstra 的加法改为 max，dist 表示到该格路径的最小瓶颈。', bruteForce:'枚举所有简单路径并计算最大边权，网格中路径数量呈指数增长。', approach:['将起点体力设为 0，其余为无穷。','最小堆按当前瓶颈值取格子。','枚举四个合法邻格。','新代价为 max(当前代价, 两格高度差)。','若更优则松弛入堆，到终点时可直接返回。'], code:'import heapq\nclass Solution:\n    def minimumEffortPath(self, heights: list[list[int]]) -> int:\n        m, n = len(heights), len(heights[0]); dist = [[float("inf")]*n for _ in range(m)]\n        dist[0][0] = 0; heap = [(0,0,0)]\n        while heap:\n            effort, r, c = heapq.heappop(heap)\n            if (r,c) == (m-1,n-1): return effort\n            if effort != dist[r][c]: continue\n            for dr,dc in ((1,0),(-1,0),(0,1),(0,-1)):\n                nr,nc=r+dr,c+dc\n                if 0<=nr<m and 0<=nc<n:\n                    ne=max(effort,abs(heights[r][c]-heights[nr][nc]))\n                    if ne<dist[nr][nc]: dist[nr][nc]=ne; heapq.heappush(heap,(ne,nr,nc))\n        return 0', walkthrough:{input:'[[1,3],[2,4]]',steps:['起点代价为 0。','到下方 2 的瓶颈为 1，到右方 3 为 2。','先弹出 2，并更新终点 4 的瓶颈为 2。','终点以代价 2 弹出，此值已最优。'],result:'返回 2。'}, complexity:{time:'O(mn log(mn))。',space:'O(mn)。'}, pitfalls:['路径代价使用 max 聚合而不是求和。','只有终点从最小堆弹出时才能确认最优。'], related:['743 网络延迟时间','778 水位上升的泳池中游泳'] },
1971: { id:1971, title:'寻找图中是否存在路径', summary:'判断无向图中 source 与 destination 是否属于同一连通分量。', constraints:['节点编号为 0 到 n-1。','图可能含环、重复路径或孤立节点。'], examples:[{input:'n=3, edges=[[0,1],[1,2],[2,0]], source=0, destination=2',output:'true',explanation:'0 可直接或经 1 到达 2。'}], intuition:'可达性只需从源点遍历其连通分量；一旦访问到目标即可结束。', bruteForce:'枚举源点到目标的所有路径会在含环图中重复探索，甚至无法自然终止。', approach:['根据边建立双向邻接表。','队列放入 source，并立即标记访问。','每次弹出一个节点。','若为 destination 则返回 true。','把所有未访问邻居标记并入队，耗尽后返回 false。'], code:'from collections import deque\nclass Solution:\n    def validPath(self, n: int, edges: list[list[int]], source: int, destination: int) -> bool:\n        graph = [[] for _ in range(n)]\n        for u,v in edges: graph[u].append(v); graph[v].append(u)\n        q=deque([source]); seen={source}\n        while q:\n            u=q.popleft()\n            if u==destination: return True\n            for v in graph[u]:\n                if v not in seen: seen.add(v); q.append(v)\n        return False', walkthrough:{input:'n=4, edges=[[0,1],[1,2]], source=0, destination=3',steps:['建立 0-1-2 分量，节点 3 孤立。','队列从 0 开始。','依次访问 1、2，并标记防止回走。','队列耗尽仍未访问节点 3。'],result:'返回 false。'}, complexity:{time:'O(V+E)。',space:'O(V+E)。'}, pitfalls:['无向边必须加入两个方向。','访问标记应在入队时设置以避免重复。'], related:['547 省份数量','841 钥匙和房间'] },
841: { id:841, title:'钥匙和房间', summary:'从 0 号房间出发收集钥匙，判断最终能否进入所有房间。', constraints:['最初只有 0 号房间已解锁。','房间内可能有重复钥匙或指向已访问房间的钥匙。'], examples:[{input:'rooms=[[1],[2],[3],[]]',output:'true',explanation:'访问顺序可为 0→1→2→3。'}], intuition:'房间是节点，钥匙是有向边；问题就是从节点 0 能否遍历到所有节点。', bruteForce:'反复扫描所有已开房间寻找新钥匙也能完成，但会多次处理相同钥匙和房间。', approach:['建立只含房间 0 的栈与访问集合。','弹出一个已解锁房间。','遍历其中全部钥匙。','遇未访问房间立即标记并压栈。','遍历结束后比较访问数量与房间总数。'], code:'class Solution:\n    def canVisitAllRooms(self, rooms: list[list[int]]) -> bool:\n        seen = {0}; stack = [0]\n        while stack:\n            room = stack.pop()\n            for key in rooms[room]:\n                if key not in seen: seen.add(key); stack.append(key)\n        return len(seen) == len(rooms)', walkthrough:{input:'rooms=[[1,2],[2],[3],[]]',steps:['进入房间 0，得到钥匙 1、2。','访问房间 2，得到钥匙 3。','访问房间 3，没有新钥匙。','再处理房间 1，其钥匙 2 已使用，四间均访问。'],result:'返回 true。'}, complexity:{time:'O(V+K)，K 为钥匙总数。',space:'O(V)。'}, pitfalls:['不能默认房间编号顺序可进入。','重复钥匙必须借助 seen 去重。'], related:['1971 寻找图中是否存在路径','图的深度优先搜索'] },
934: { id:934, title:'最短的桥', summary:'把二进制网格中的两座岛用最少数量的海水格连接起来。', constraints:['网格恰好包含两座由四方向相连的岛。','只能把值为 0 的海水格改为 1。'], examples:[{input:'grid=[[0,1],[1,0]]',output:'1',explanation:'翻转任一中间相邻海格即可连接两岛。'}], intuition:'先完整标记第一座岛，并把岛上所有格同时作为 BFS 起点；逐层扩张海水，首次碰到第二岛时层数最少。', bruteForce:'从第一岛每个格分别 BFS 到第二岛，会重复搜索大量相同海域。', approach:['扫描并找到第一座岛的任一格。','DFS 标记整座岛并把所有岛格加入队列。','以多源 BFS 按层扩展。','未访问海格标记后入队。','首次遇到未标记的陆地时返回已扩展海层数。'], code:'from collections import deque\nclass Solution:\n    def shortestBridge(self, grid: list[list[int]]) -> int:\n        n=len(grid); q=deque(); seen=set()\n        def dfs(r,c):\n            if not (0<=r<n and 0<=c<n) or (r,c) in seen or grid[r][c]==0: return\n            seen.add((r,c)); q.append((r,c))\n            for dr,dc in ((1,0),(-1,0),(0,1),(0,-1)): dfs(r+dr,c+dc)\n        found=False\n        for r in range(n):\n            for c in range(n):\n                if grid[r][c]: dfs(r,c); found=True; break\n            if found: break\n        steps=0\n        while q:\n            for _ in range(len(q)):\n                r,c=q.popleft()\n                for dr,dc in ((1,0),(-1,0),(0,1),(0,-1)):\n                    nr,nc=r+dr,c+dc\n                    if 0<=nr<n and 0<=nc<n and (nr,nc) not in seen:\n                        if grid[nr][nc]: return steps\n                        seen.add((nr,nc)); q.append((nr,nc))\n            steps+=1', walkthrough:{input:'[[0,1],[1,0]]',steps:['DFS 找到并标记右上角第一岛。','把该岛格作为 BFS 第 0 层。','检查相邻海格并准备扩张。','其中海格紧邻左下第二岛，首次接触时需翻一格。'],result:'返回 1。'}, complexity:{time:'O(n²)。',space:'O(n²)。'}, pitfalls:['BFS 起点应包含第一岛的全部格子。','steps 统计翻转海格数，遇第二岛时不要再加一。'], related:['200 岛屿数量','1091 二进制矩阵中的最短路径'] },
752: { id:752, title:'打开转盘锁', summary:'四位转盘每次可将一位拨动一格，避开死亡状态求到目标的最少步数。', constraints:['每位数字 0 与 9 首尾相邻。','起点为 0000，死亡状态不能进入；起点死亡则无解。'], examples:[{input:'deadends=["0201","0101","0102","1212","2002"], target="0202"',output:'6',explanation:'存在六次拨动到达目标且避开禁用状态的路线。'}], intuition:'每个合法组合是无权图节点，每次拨一位形成八个邻居，BFS 首次到目标即为最短步数。', bruteForce:'DFS 尝试所有拨法，容易在环中来回并且不能保证先得到最短路线。', approach:['把死亡状态放入 blocked 集合。','若起点被禁用则返回 -1。','队列保存状态与步数，起点先标记。','对四个位置分别生成加一、减一邻居。','合法未访问邻居入队，首次取到目标时返回步数。'], code:'from collections import deque\nclass Solution:\n    def openLock(self, deadends: list[str], target: str) -> int:\n        blocked=set(deadends); start="0000"\n        if start in blocked: return -1\n        q=deque([(start,0)]); seen={start}\n        while q:\n            state,d=q.popleft()\n            if state==target: return d\n            for i,ch in enumerate(state):\n                digit=int(ch)\n                for nd in ((digit+1)%10,(digit-1)%10):\n                    nxt=state[:i]+str(nd)+state[i+1:]\n                    if nxt not in blocked and nxt not in seen: seen.add(nxt); q.append((nxt,d+1))\n        return -1', walkthrough:{input:'deadends=[], target="0009"',steps:['起点 0000 的距离为 0。','枚举最后一位向前拨得到 0001。','向后拨时 0 环绕为 9，得到 0009。','0009 以距离 1 入队并随后被取出。'],result:'返回 1。'}, complexity:{time:'O(10⁴·4)，状态空间固定。',space:'O(10⁴)。'}, pitfalls:['数字拨减时 0 必须环绕到 9。','死亡集合含 0000 时不能开始搜索。'], related:['127 单词接龙','1091 二进制矩阵中的最短路径'] },
1091: { id:1091, title:'二进制矩阵中的最短路径', summary:'在方阵中沿八个方向经过值为 0 的格子，求左上到右下最短路径长度。', constraints:['允许横、竖、斜共八个方向移动。','起点或终点被阻塞时不存在路径，长度按经过格子数计算。'], examples:[{input:'grid=[[0,1],[1,0]]',output:'2',explanation:'可从左上斜向走到右下。'}], intuition:'每次移动代价相同，BFS 的层次就是路径长度；访问过的格子无需再次进入。', bruteForce:'枚举所有简单路径会因八方向分支而快速爆炸。', approach:['先检查起点和终点是否为 0。','队列放入起点与长度 1。','每次弹出格子并检查是否终点。','枚举周围八个方向的合法空格。','入队时原地标记访问，队列耗尽则返回 -1。'], code:'from collections import deque\nclass Solution:\n    def shortestPathBinaryMatrix(self, grid: list[list[int]]) -> int:\n        n=len(grid)\n        if grid[0][0] or grid[n-1][n-1]: return -1\n        q=deque([(0,0,1)]); grid[0][0]=1\n        while q:\n            r,c,d=q.popleft()\n            if (r,c)==(n-1,n-1): return d\n            for dr in (-1,0,1):\n                for dc in (-1,0,1):\n                    nr,nc=r+dr,c+dc\n                    if 0<=nr<n and 0<=nc<n and grid[nr][nc]==0:\n                        grid[nr][nc]=1; q.append((nr,nc,d+1))\n        return -1', walkthrough:{input:'[[0,1],[1,0]]',steps:['起点与终点均开放。','队列以 (0,0,1) 开始。','八方向枚举发现右下角可斜向进入。','终点以长度 2 出队。'],result:'返回 2。'}, complexity:{time:'O(n²)。',space:'O(n²)，最坏队列保存大量格子。'}, pitfalls:['路径长度初始是 1 而非 0。','必须包含对角方向，并在入队时标记。'], related:['934 最短的桥','752 打开转盘锁'] },
733: { id:733, title:'图像渲染', summary:'从指定像素开始，把与其四方向连通且颜色相同的区域改成新颜色。', constraints:['只有与起点同色且四连通的像素会改变。','新颜色可能与原颜色相同。'], examples:[{input:'image=[[1,1,1],[1,1,0],[1,0,1]], sr=1,sc=1,color=2',output:'[[2,2,2],[2,2,0],[2,0,1]]',explanation:'右下角的 1 与起点不四连通，保持不变。'}], intuition:'目标区域就是从起点沿同色边可达的连通分量，DFS/BFS 访问并原地染色即可。', bruteForce:'反复扫描整张图寻找与已染区域相邻的原色像素，会产生许多无效全图遍历。', approach:['记录起点旧颜色。','若新旧颜色相同直接返回，避免无限递归。','从起点开始 DFS。','将当前格改色后检查四邻格。','仅对边界内且仍为旧颜色的邻格递归。'], code:'class Solution:\n    def floodFill(self, image: list[list[int]], sr: int, sc: int, color: int) -> list[list[int]]:\n        old=image[sr][sc]\n        if old==color: return image\n        m,n=len(image),len(image[0])\n        def dfs(r,c):\n            if not (0<=r<m and 0<=c<n) or image[r][c]!=old: return\n            image[r][c]=color\n            for dr,dc in ((1,0),(-1,0),(0,1),(0,-1)): dfs(r+dr,c+dc)\n        dfs(sr,sc); return image', walkthrough:{input:'image=[[1,1],[1,0]], start=(0,0), color=2',steps:['起点旧颜色为 1。','先把 (0,0) 改为 2。','递归染色右侧 (0,1)。','再染色下侧 (1,0)，值 0 的格子被边界条件挡住。'],result:'得到 [[2,2],[2,0]]。'}, complexity:{time:'O(mn)。',space:'O(mn)，最坏递归栈覆盖整个图。'}, pitfalls:['新旧颜色相同必须提前返回。','只能四方向连通，斜对角不算。'], related:['200 岛屿数量','547 省份数量'] },
463: { id:463, title:'岛屿的周长', summary:'在只有一座岛的网格中计算陆地与海水或边界接触的边总数。', constraints:['陆地通过四方向连接，网格外视为海水。','没有湖泊这一条件不影响逐边计数方法。'], examples:[{input:'grid=[[0,1,0],[1,1,1],[0,1,0]]',output:'12',explanation:'逐个陆地格统计朝向海水或边界的边，共 12。'}], intuition:'每块陆地有四条候选边；某方向越界或邻格为水，这条边就贡献一个单位周长。', bruteForce:'沿岛屿轮廓模拟行走需要处理转向、起点和凹角，复杂且易漏。', approach:['遍历网格中的每个格子。','水格直接跳过。','对陆地枚举上下左右四个方向。','若邻坐标越界则周长加一。','若邻格为水也加一，最后返回累计值。'], code:'class Solution:\n    def islandPerimeter(self, grid: list[list[int]]) -> int:\n        m,n=len(grid),len(grid[0]); ans=0\n        for r in range(m):\n            for c in range(n):\n                if grid[r][c]==0: continue\n                for dr,dc in ((1,0),(-1,0),(0,1),(0,-1)):\n                    nr,nc=r+dr,c+dc\n                    if not (0<=nr<m and 0<=nc<n) or grid[nr][nc]==0: ans+=1\n        return ans', walkthrough:{input:'grid=[[1,1]]',steps:['访问左侧陆地，它的上、下、左三边外露。','其右边接触另一块陆地，不计。','访问右侧陆地，上、下、右三边外露。','两格之间共享边始终不计。'],result:'周长为 6。'}, complexity:{time:'O(mn)。',space:'O(1)。'}, pitfalls:['共享陆地边不能计入周长。','网格边界外也贡献周长。'], related:['200 岛屿数量','695 岛屿的最大面积'] },
797: { id:797, title:'所有可能的路径', summary:'列出有向无环图中从节点 0 到最后一个节点的所有路径。', constraints:['输入图为 DAG，因此递归不会因环无限循环。','路径必须从 0 开始并在 n-1 结束。'], examples:[{input:'graph=[[1,2],[3],[3],[]]',output:'[[0,1,3],[0,2,3]]',explanation:'从 0 可经 1 或 2 到达 3。'}], intuition:'回溯维护当前路径；到达终点时复制路径作为一个答案，返回后撤销选择继续其他分支。', bruteForce:'本题必须输出全部路径，无法避免与输出规模相当的枚举；低效做法是每步复制整条路径。', approach:['初始化路径为 [0]。','DFS 当前路径尾节点。','若到终点则复制路径加入答案。','否则依次选择每个出邻居并压入路径。','递归返回后弹出邻居，继续下一选择。'], code:'class Solution:\n    def allPathsSourceTarget(self, graph: list[list[int]]) -> list[list[int]]:\n        target=len(graph)-1; ans=[]; path=[0]\n        def dfs(u):\n            if u==target: ans.append(path.copy()); return\n            for v in graph[u]:\n                path.append(v); dfs(v); path.pop()\n        dfs(0); return ans', walkthrough:{input:'graph=[[1,2],[3],[3],[]]',steps:['路径从 [0] 开始。','先选择 1，再选择 3，记录 [0,1,3]。','回溯两步并从 0 选择 2。','由 2 到 3，记录 [0,2,3]。'],result:'返回两条路径。'}, complexity:{time:'O(P·L)，P 为路径数、L 为平均路径长度。',space:'O(L)，不计输出；递归与当前路径同阶。'}, pitfalls:['加入答案时必须复制 path。','到达终点后应立即返回，不再扩展。'], related:['332 重新安排行程','回溯法'] },
332: { id:332, title:'重新安排行程', summary:'使用每张机票恰好一次，从 JFK 出发构造字典序最小的完整行程。', constraints:['机票是有向边，可能有重边。','题目保证至少存在一条使用全部机票的有效行程。'], examples:[{input:'tickets=[["JFK","SFO"],["JFK","ATL"],["ATL","JFK"]]',output:'["JFK","ATL","JFK","SFO"]',explanation:'该完整路线且字典序小于先去 SFO 的尝试。'}], intuition:'这是欧拉路径。不能看到最小目的地就永久决定，因为可能走进死路；应持续取最小边，在无边可走时把节点逆序加入答案。', bruteForce:'回溯枚举机票排列并选择最小完整行程，最坏达到 E!。', approach:['为每个出发机场建立目的地最小堆。','从 JFK 启动深度搜索。','当前机场还有边时反复弹出字典序最小目的地。','先递归用完该目的地之后的边。','当前无边时加入路线，最终反转后序结果。'], code:'import heapq\nfrom collections import defaultdict\nclass Solution:\n    def findItinerary(self, tickets: list[list[str]]) -> list[str]:\n        graph=defaultdict(list)\n        for src,dst in tickets: heapq.heappush(graph[src],dst)\n        route=[]\n        def visit(src):\n            while graph[src]: visit(heapq.heappop(graph[src]))\n            route.append(src)\n        visit("JFK")\n        return route[::-1]', walkthrough:{input:'JFK→KUL, JFK→NRT, NRT→JFK',steps:['JFK 的最小边先到 KUL，但 KUL 无出边。','后序先把 KUL 放入 route。','回到 JFK 后走 NRT，再由 NRT 回 JFK。','各节点在无边时后序加入，反转得到完整路线。'],result:'得到 [JFK,NRT,JFK,KUL]。'}, complexity:{time:'O(E log E)，每条边入堆和出堆一次。',space:'O(E)，邻接堆、递归栈与结果。'}, pitfalls:['直接贪心输出会被死路卡住，必须后序加入。','重复机票是不同边，不能用集合去重。'], related:['797 所有可能的路径','欧拉路径'] },
};
